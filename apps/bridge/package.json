{
  "name": "@socketlens/bridge",
  "version": "0.1.2",
  "private": true,
  "description": "Loopback bridge that lets the browser-based interface drive a raw TCP SLTP connection. Serves the built React interface and relays commands; it never reimplements SLTP.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "socketlens-bridge": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@socketlens/core": "0.1.2",
    "@socketlens/protocol": "0.1.2"
  }
}
