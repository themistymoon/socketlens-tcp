{
  "format": "socketlens-scenario-bundle/1",
  "protocol": "SLTP/1.0",
  "name": "Delayed response",
  "description": "A rule that waits before answering, so client-side timeout handling can be exercised without failing.",
  "rules": [
    {
      "id": "slow-ok",
      "name": "Answer after 400 ms",
      "priority": 10,
      "match": { "operation": "SLOW_QUERY" },
      "response": {
        "statusCode": 200,
        "statusPhrase": "OK",
        "headers": { "Content-Type": "application/json; charset=utf-8" },
        "body": "{\"rows\":3}",
        "delayMs": 400
      }
    }
  ],
  "scenarios": [
    {
      "name": "slow-but-within-timeout",
      "description": "The mock waits 400 ms and the scenario allows 3000 ms, so the test passes. The recorded duration proves the delay was real.",
      "request": { "operation": "SLOW_QUERY" },
      "timeoutMs": 3000,
      "expect": {
        "statusCode": 200,
        "statusPhrase": "OK",
        "jsonSubset": { "rows": 3 }
      }
    }
  ]
}
